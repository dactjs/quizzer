export const ENDPOINTS = {
  USERS: "/api/users",
  QUIZZES: "/api/quizzes",
  CONVOCATORIES: "/api/convocatories",
  CERTIFICATES: "/api/certificates",
} as const;

export default ENDPOINTS;
